# React-Express Website Project

This example shows a minimal React front-end served by a Node.js backend.

## Usage

```
npm start
```

Then open `http://localhost:3000` in a browser.

The app lets you view and add simple items via an API.
